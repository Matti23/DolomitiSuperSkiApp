name: dolomiti_ski_app
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^8.2.2
  latlong2: ^0.9.1
  geolocator: ^10.0.0
  flutter_compass: ^0.8.1
  http: ^1.1.0
  sqflite: ^2.0.0
  path_provider: ^2.0.0
  cupertino_icons: ^1.0.8
  flutter_map_tile_caching: ^10.1.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
